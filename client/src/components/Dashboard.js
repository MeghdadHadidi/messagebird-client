import React from 'react';

import Widget from './Widget'

const Dashboard = () => {
    return (
        <React.Fragment>
            <Widget />
        </React.Fragment>
    );
};

export default Dashboard;